const autenticado = true;

if (autenticado) {
  console.log('Si puedes usar el app');
} else {
  console.log('No puedes usar el app');
}

// Ternario
console.log( autenticado ? 'Si puedes usar el app' : 'No puedes usar el app');

